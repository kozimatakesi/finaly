import React from 'react';
import { Input, Box, Button } from '@chakra-ui/react';

const ImportArea = () => {
  const a = 0;
  return (
    <Box>
      <Input />
      <Button>読み込み</Button>
    </Box>
  );
};

export default ImportArea;
